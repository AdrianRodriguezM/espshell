/* SPDX-License-Identifier: GPL-3.0-or-later
 * project_app.c — empty by default. This is where downstream projects layer
 * their own command handlers and tasks on top of the core. See README and
 * components/core/include/cmd.h for the registration API.
 */
#include "project_app.h"

void project_init(void)
{
    /* No project-specific commands in the bare framework. */
}
